`<%= templates %>`: Separate template for mobile is used.
`<%= foundMomentumScrollRule %>`: Scrollable elements are optimized for mobile with a momentum scroll.
`<%= foundDisableSelectRule %>`: Elements are optimized for mobile selection behavior.